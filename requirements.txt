# Requirements for the setup wizard. Tkinter ships with Python on
# Windows/macOS; Linux needs python3-tk (apt install python3-tk).
# Node.js is NOT required (asar patching is pure Python).

pillow>=10.1      # per-instance colored icons
pyinstaller>=6.0  # packaging only
